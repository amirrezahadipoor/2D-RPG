# ارزیابی سخت‌گیرانه‌ی وضعیت فعلی بازی — از ۰ تا ۱۰۰۰

> این ارزیابی از روی **کد واقعی repo** (نه ادعاها) انجام شده: خواندن سورس، شمارش
> دیتابیس‌ها، رندر واقعی اسکرین‌شات‌ها (`docs/screenshots/*.png`) و اجرای تست‌ها.
> هدف این سند دو تاست: (۱) امتیاز صادقانه، (۲) تبدیل هر کسر امتیاز به یک بند
> اجرایی در `ROADMAP_1000.md` تا بشود واقعاً به ۱۰۰۰ رسید.

## جدول امتیاز نهایی

| # | معیار | وزن | امتیاز | نتیجه از ۱۰۰۰ |
|---|---|---|---|---|
| 1 | فارسی‌سازی کامل | 30 | 27 | کامل: ۳۱۶ کلید en=fa، صفر مورد ناترجمه یا افتاده |
| 2 | زیبایی/تنوع رنگی پیکسل‌آرت | 55 | 20 | پالت محدود عمداً (خوب برای سبک) ولی سطح جزئیات پایین است |
| 3 | کفایت انیمیشن‌ها | 40 | 13 | فقط ۸ فریم بدنی (۲ idle+۴ walk+۲ attack)، بدون dodge/hurt/death فریم اختصاصی |
| 4 | کیفیت گیم‌پلی هسته‌ای | 60 | 34 | کمبو، هوی‌اتک، دوج، پری واقعی پیاده شده؛ ولی طیف اکشن محدود |
| 5 | بالانس اقتصاد/دمیج/سختی | 45 | 34 | مستند و پایدار (`docs/BALANCE.md`) ولی تست نشده روی بازیکن واقعی |
| 6 | ساعت‌های واقعی گیم‌پلی | 35 | 15 | فقط ۶ عمق دانجن، دنیای ثابت کوچک؛ کوئست دیتا کافی ولی محتوای دستی کم |
| 7 | صحت آماری ۱۰۰ main quest | 20 | 20 | تأیید کد: `CHAPTERS(10) * STAGES(10) = 100` واقعی |
| 8 | صحت آماری ۳۰۰ side quest | 20 | 20 | تأیید کد: `SIDE_COUNT = 300` واقعی |
| 9 | کوئست از NPC گرفته می‌شود | 30 | 27 | بله، `dialogue.gd` واقعاً quest offer/turn-in با NPC مسیردار دارد |
| 10 | عمق/تنوع مکالمات NPC | 30 | 16 | فقط یک خط hello ثابت به‌ازای نقش (۵ نقش)، بدون شخصیت فردی |
| 11 | نمایش تجهیزات روی هیرو | 30 | 28 | paper-doll واقعی، لایه‌به‌لایه، هم‌زمان با انیمیشن |
| 12 | تنوع/کمیابی سیستم آیتم | 45 | 17 | فقط ۴ رتبه کمیابی، ~۲۹ آیتم پایه، بدون ست‌آیتم/legendary واقعی |
| 13 | جایگاه جهانی بازی | 30 | 1 | پروژه شخصی، منتشرنشده در استورهای عمومی، بدون بازخورد/رتبه واقعی |
| 14 | کامل‌بودن و اتصال منطق (سیم‌کشی) | 45 | 30 | تست‌ها ۵۷۱/۵۷۱ پاس؛ ولی باگ‌های سکوت (پایین) پیدا شد |
| 15 | جذابیت روایت داستان | 45 | 12 | کوئست‌ها الگوریتمی/رویه‌ای‌اند، بدون شخصیت‌پردازی یا twist روایی |
| 16 | چینش عاقلانه‌ی نقشه‌ی جهان | 55 | 20 | جای‌گیری نیمه‌رندوم با قید فاصله؛ بایوم‌ها/جاده منطقی ولی بدون طراحی دستی |
| 17 | زیبایی/جزئیات خانه‌ها | 30 | 10 | داخل خانه فقط اتاق ساده + راهرو با چند مبل، صفر شخصیت معماری |
| 18 | حس ۲.۵بعدی | 25 | 17 | سایه‌ی بیضی زیر اکتورها + دیوار با rim-light هست؛ عمق واقعی محدود |
| 19 | جذابیت سیستم لول‌آپ/تلنت | 40 | 10 | فقط ۳ شاخه‌ی تلنت خطی (might/vigor/swift)، بدون درخت واقعی |
| 20 | سختی متعادل و جذاب | 35 | 23 | فرمول‌های TTK مستند و منطقی، الیت‌ها واقعی |
| 21 | رفتار طبیعی/زنده‌ی NPCها | 30 | 25 | زمان‌بندی روزانه‌ی واقعی (خواب/بازار/مزرعه) + جداسازی فیزیکی از هم |
| 22 | ورود به خانه + تعامل با NPC داخل | 30 | 6 | ورود به خانه کار می‌کند اما **هیچ NPC داخل خانه‌ها نیست** — فقط مبلمان |
| 23 | ساخت واقعی قصر/شهر/روستا | 30 | 24 | کاخ پادشاه در تاون + خانه‌های واقعی روستا/شهر با درب و تودرتو |
| 24 | بزرگی جهان + رمزوراز/آیتم مخفی | 40 | 12 | نقشه ۳۸۴×۲۵۶؛ دیوار مخفی و صندوق مخفی وجود دارد ولی کم و تکراری |
| 25 | افکت ویژه‌ی آیتم‌های نایاب | 30 | 5 | **باگ واقعی پیدا شد:** شرط glow روی `rarity>=4` است اما بیشینه‌ی رتبه ۳ است → هیچ آیتمی هرگز glow نمی‌گیرد |
| 26 | سلامت فنی (بدون دِدکد/باگ پنهان) | 25 | 14 | باگ بالا + نبود NPC داخل خانه‌ها نشان می‌دهد قطعات یتیم هنوز هست |
| 27 | تطابق کامل با اندروید | 30 | 27 | APK واقعی روی CI ساخته و امضا می‌شود، ۵۷۱ تست هدلس+ویندویید پاس |
| 28 | بی‌نیازی از کیبورد/کنترل نامتناسب | 30 | 26 | لمسی‌محور واقعی (tap/drag/flick)، کیبورد فقط fallback دسکتاپ |
| 29 | سطح کلی هنر پیکسلی | 35 | 11 | تولید پروسیجرال با PIL، نه دست‌کشیده؛ خوانا ولی کم‌جزئیات و کلیشه‌ای |
| 30 | جای‌گیری درست تمام سیستم‌ها (UX) | 30 | 21 | HUD/Inventory/Journal/Talents/Shop هرکدام جای خودشان‌اند و کار می‌کنند |
| | **جمع** | **1055** | **565** | **≈ ۵۳۶ از ۱۰۰۰** |

## نمره‌ی نهایی نرمال‌شده: **۵۳۶ / ۱۰۰۰**

### چرا نه بیشتر:
بازی از نظر **مهندسی نرم‌افزار** (CI سبز، تست پوششی ۵۷۱، معماری تمیز autoload/data/entity،
لوکالایز کامل، اکسپورت اندروید واقعی) در سطح بسیار خوبی است. اما از نظر **محتوا و طراحی
تجربه‌ی بازیکن** — که وزن غالب سؤالات شما را تشکیل می‌دهد — بازی هنوز یک **اسکلت رویه‌ای
(procedural skeleton)** است، نه یک بازی نقش‌آفرینی جهان‌باز کامل با شخصیت:
- کوئست‌ها با فرمول تولید می‌شوند، نه با متن دست‌نویس یا شاخه‌های روایی.
- NPCها فقط یک جمله‌ی سلام ثابت دارند و داخل خانه‌هایشان اصلاً وجود ندارند.
- تنوع آیتم فقط از ترکیب ~۵ پیشوند/پسوند روی چند ده آیتم پایه می‌آید — هیچ آیتم دست‌ساز
  منحصر‌به‌فرد با افکت واقعی گیم‌پلی (نه فقط رنگ) وجود ندارد.
- تلنت فقط ۳ خط ساده‌ی خطی است، نه یک درخت با انتخاب معنادار.
- جهان کوچک است (۶ عمق دانجن، ۴ سکونتگاه) — برای صرف "چند ده ساعت" واقعی کافی نیست مگر با
  grind کوئست‌های تکراری.
- بازی هرگز منتشر/رتبه‌بندی عمومی نشده — امتیاز "رتبه جهانی" عملاً صفر است چون معیار قابل
  اندازه‌گیری‌ای وجود ندارد.

### باگ‌های واقعی جدید کشف‌شده در همین بررسی (که در روادمپ زیر رفع می‌شوند):
1. **Glow ویژه فقط مخصوص ۳ رلیک است، نه برای rare/epic معمولی** — `pickup.gd:23` شرط
   `rarity >= 4` است. بررسی دقیق‌تر نشان داد `ItemGen.roll_rarity()` حداکثر ۳ (epic)
   برمی‌گرداند، اما `Inventory.claim_artifact()` رلیک‌های صندوق مخفی را با `"rarity": 4`
   می‌سازد — پس این glow فقط برای آن ۳ آیتم رلیک واقعاً اجرا می‌شود، نه دِدکد کامل. مشکل
   واقعی این است که **آیتم epic معمولی (rarity=3) از دشمن/صندوق هرگز افکت ویژه‌ی بصری
   نمی‌گیرد** - فقط رنگ بنفش تیره‌تر دارد، هیچ glow/particle اضافه‌ای ندارد. این نکته در
   R4.1/R4.2 اصلاح می‌شود (رتبه‌ی legendary جدید + افکت گیم‌پلی واقعی، نه فقط بصری).
2. **هیچ NPC داخل خانه‌ها نیست** — `Interior` فقط مبلمان می‌سازد؛ NPCها هرگز به داخل بازه‌ی
   زمانی «home» منتقل نمی‌شوند (روی نقشه‌ی جهان می‌مانند)، پس "ورود به خانه‌ها برای تعامل
   با NPC" که کاربر پرسید در حال حاضر **دروغ است اگر ادعا شود پیاده شده**.

این دو یافته‌ی تازه به بالای `ROADMAP_1000.md` اضافه شدند تا اول رفع شوند.
