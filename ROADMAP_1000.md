# نقشه‌راه رسیدن از ۵۳۶ به ۱۰۰۰ (بر اساس SCORE_REPORT.md)

قانون این نقشه‌راه مثل ROADMAP.md اصلی است: **هیچ بندی بدون اجرای واقعی و تست عبوری تیک
نمی‌خورد.** بعد از هر فاز: `godot --headless --path . --import` (دو بار) +
`godot --headless --path . res://tests/verify.tscn` باید صفر خطا و همه‌چک‌ها PASS بدهد، و
باید commit+push شود و CI روی GitHub سبز شود.

هر فاز وزنی از ۱۰۰۰ به آن اختصاص دارد که تقریبی و برای اولویت‌بندی است، نه قرارداد دقیق.

---

## فاز R0 — رفع دو باگ واقعی کشف‌شده (فوری، وزن ~۱۵)
- [ ] R0.1 رفع دِدکد glow: یا آستانه در `pickup.gd` را به `rarity >= 3` (epic) تغییر بده،
      یا یک رتبه‌ی پنجم `legendary=4` واقعی به `ItemGen` اضافه کن (ترجیح: رتبه‌ی پنجم، چون
      همزمان باعث تنوع آیتم بیشتر هم می‌شود — رجوع به R4).
- [ ] R0.2 حداقل یک NPC واقعی داخل هر Interior بگذار (صاحب‌خانه) با دیالوگ مخصوص، تا ادعای
      "می‌شود وارد خانه‌ها شد و با NPC تعامل کرد" واقعاً درست باشد.

## فاز R1 — عمق مکالمات NPC (وزن ~۶۰)
- [ ] R1.1 هر NPC یک "شخصیت" (personality seed) بگیرد: ۱۰-۱۲ خط دیالوگ idle متفاوت
      به‌ازای هر نقش (نه فقط یک خط ثابت)، چرخشی بر اساس روز/ساعت/بایوم.
- [ ] R1.2 دیالوگ‌های ویژه برای وقایع دنیا: مثلاً بعد از کشتن یک boss دانجن، NPCهای شهر
      نزدیک یک خط تازه بگویند (سیستم "شایعه" ساده: پرچم‌های جهانی که چند خط دیالوگ را
      باز می‌کنند).
- [ ] R1.3 پادشاه/الدرها در طول ۱۰ چپتر داستان اصلی خط‌های متفاوت per-chapter بگویند، نه
      فقط یک act_line عمومی.

## فاز R2 — روایت داستان جذاب‌تر (وزن ~۷۰)
- [ ] R2.1 برای هر ۱۰ چپتر (نه هر ۱۰۰ مرحله) یک خط داستانی دستی/طراحی‌شده در دو زبان
      بنویس که پیش‌زمینه، انگیزه، و twist را بیان کند؛ الان `story.act.N` فقط ۱۰ خط ساده دارد.
- [ ] R2.2 یک "نمو" (arc) واحد بساز: Ember Crown، معنای آن، دشمن اصلی (اژدها؟)، انگیزه‌ی
      پادشاه — این‌ها الان فقط اسم هستند، پشتشان روایت نیست.
- [ ] R2.3 حداقل ۳ کات‌سین کوتاه متنی/بصری (شروع، وسط، پایان) که با `cutscene` گروه موجود
      کار کند.

## فاز R3 — چینش عاقلانه‌ی نقشه‌ی جهان (وزن ~۵۰)
- [ ] R3.1 به‌جای جای‌گیری کاملاً رندومِ ۴ سکونتگاه با قید فاصله، یک چیدمان **دستی‌طراحی‌شده
      اما پارامتری** بساز: پایتخت مرکزی، ۳ روستا در ۳ جهت با منطق بیوم (روستای جنگل نزدیک
      جنگل، روستای صحرا نزدیک صحرا و غیره) — یعنی anchor نیمه‌ثابت + رندوم محدود اطراف آن.
- [ ] R3.2 جاده‌ها را از خط L-شکل ساده به مسیر منطقی‌تر (حداقل رعایت مانع/آب) ارتقا بده.
- [ ] R3.3 نقاط دیدنی (shrine/camp/signpost) را طوری قرار بده که در طول جاده‌ی اصلی هر
      چند صد پیکسل یک‌بار دیده شوند، نه صرفاً پراکنده‌ی تصادفی.
- [ ] R3.4 در `README.md`/`docs` نقشه‌ی جهان را به‌صورت تصویری (overview render) مستند کن.

## فاز R4 — تنوع/کمیابی و افکت آیتم (وزن ~۶۰)
- [ ] R4.1 رتبه‌ی پنجم `legendary` با نرخ افت بسیار پایین (~۰.۵٪) اضافه کن؛ فقط از باس‌های
      دانجن یا صندوق‌های مخفی.
- [ ] R4.2 هر آیتم legendary یک افکت گیم‌پلی واقعی داشته باشد (نه فقط رنگ): مثلاً +سرعت
      حمله، بازگشت استقامت روی کشتن، شانس بیشتر دراپ. باید در `Stats`/`Hero` واقعاً اثر بگذارد.
- [ ] R4.3 حداقل ۳-۵ آیتم "یونیک نام‌دار" (نه پیشوند/پسوند رویه‌ای) با نام و توضیح دست‌نویس
      دوزبانه اضافه کن.
- [ ] R4.4 پیشوند/پسوندهای بیشتر (الان فقط ۵+۴) برای تنوع بصری-عددی بیشتر.

## فاز R5 — سیستم تلنت واقعی‌تر (وزن ~۴۵)
- [ ] R5.1 از ۳ خط ساده (might/vigor/swift) به یک درخت کوچک واقعی با انشعاب برو: مثلاً هر
      شاخه بعد از رتبه‌ی ۵ یک "گره انتخابی" باز کند (مثلاً "خونریزی حمله" یا "ضدحمله خودکار").
- [ ] R5.2 حداقل یک قابلیت فعال (نه فقط پسیو عددی) از طریق تلنت باز شود.
- [ ] R5.3 UI تلنت‌ها را برای نمایش شاخه‌ها/گره‌ها به‌روزرسانی کن.

## فاز R6 — انیمیشن (وزن ~۵۵)
- [ ] R6.1 فریم‌های اختصاصی dodge (الان از walk بازاستفاده می‌شود احتمالاً) اضافه کن.
- [ ] R6.2 فریم hurt/hit-reaction و فریم death مجزا برای هیرو و دشمنان.
- [ ] R6.3 برای حداقل دشمنان اصلی (goblin/skeleton/demon/dragon) انیمیشن attack مجزا از
      حرکت اضافه کن.
- [ ] R6.4 گرید انیمیشن از ۸ فریم به حداقل ۱۲-۱۴ فریم در تعریف `tools/gen_assets.py` گسترش
      یابد و همه‌ی لایه‌های تجهیزات هم‌زمان به‌روزرسانی شوند.

## فاز R7 — سطح هنر / تنوع رنگی (وزن ~۹۰)
- [ ] R7.1 پالت را برای هر بایوم متمایزتر کن (الان بایوم‌ها بیشتر tint هستند تا طرح متفاوت).
- [ ] R7.2 جزئیات ثانویه (سایه‌ی چندلایه، هایلایت لبه، بافت زمین متنوع‌تر) به `gen_assets.py`
      اضافه کن.
- [ ] R7.3 خانه‌ها را از مستطیل سقف قرمز ساده به طرح‌های متنوع (رنگ سقف/دیوار متفاوت به
      نوع سکونتگاه/نقش ساکن) ارتقا بده.
- [ ] R7.4 افکت‌های ذره‌ای بیشتر برای کست/کریتیکال/باران/برف برای تقویت حس ۲.۵بعدی.

## فاز R8 — بزرگی جهان و رمزوراز (وزن ~۵۰)
- [ ] R8.1 حداقل ۲ برابر تعداد فعلی دیوار مخفی/صندوق مخفی/کمین در سراسر دانجن‌ها و جهان.
- [ ] R8.2 یک یا دو "دانجن ویژه‌ی پنهان" اضافه کن که فقط با حل یک سرنخ در دنیای اصلی
      (مثلاً یک signpost خاص یا NPC) پیدا شود.
- [ ] R8.3 نقشه‌ی جهان را کمی بزرگ‌تر کن یا تراکم محتوا (POI) را بیشتر کن تا کاوش ارزش
      وقت بیشتری داشته باشد.

## فاز R9 — زیبایی/جزئیات خانه‌ها و ورود/تعامل (وزن ~۴۵)
- [ ] R9.1 هر نوع خانه (`home`/`town_house`/`palace`) طرح داخلی متفاوت‌تری بگیرد (نه فقط
      اندازه‌ی اتاق، بلکه چیدمان مبلمان و دکور اختصاصی).
- [ ] R9.2 صاحب‌خانه‌ی واقعی (از R0.2) دیالوگ و شیدول داشته باشد: شب‌ها در خانه، روز بیرون —
      و اگر بازیکن روز وارد شود خانه خالی باشد (منطقی‌تر از همیشه NPC ایستاده).
- [ ] R9.3 تعامل با اشیای داخل خانه (صندوق شخصی، کتاب، تخت) با فیدبک بصری بهتر.

## فاز R10 — ساعت‌های گیم‌پلی و محتوای دستی (وزن ~۴۵)
- [ ] R10.1 برای main quest chapter boundary (هر ۱۰ مرحله) یک "چالش دستی" (نه فقط
      kill/collect/clear رویه‌ای) طراحی کن: پازل ساده، escort، یا انتخاب اخلاقی.
- [ ] R10.2 دانجن عمق را از ۶ به حداقل ۸-۱۰ افزایش بده یا یک "دانجن بی‌نهایت اختیاری"
      (endless/roguelite mode) بعد از پایان اصلی داستان اضافه کن تا طول عمر بازی برای
      بازیکنان پیگیر بیشتر شود.

## فاز R11 — QA نهایی و اعتبارسنجی مجدد (اجباری قبل از ادعای اتمام هر فاز)
- [ ] R11.1 بعد از هر فاز بالا، امتیازدهی SCORE_REPORT.md را به‌روزرسانی کن (نه فقط تیک
      روادمپ) تا عدد /۱۰۰۰ واقعی و رهگیری‌پذیر بماند.
- [ ] R11.2 اسکرین‌شات‌های `docs/screenshots/*` را بعد از تغییرات بصری بزرگ (R6, R7, R9)
      دوباره بگیر تا مستندات با واقعیت هم‌خوانی داشته باشد.
- [ ] R11.3 پوش‌های سریع و پیاپی، هر پوش با تأیید سبز شدن واقعی CI (نه فقط لوکال) قبل از
      رفتن سراغ فاز بعد.

---

### نکته درباره‌ی "جایگاه/رتبه‌ی جهانی" (سؤال ۱۳ کاربر)
این مورد در کنترل مستقیم کد نیست — نیازمند انتشار واقعی در استور (مثل Cafe Bazaar/Google
Play)، بازخورد کاربران واقعی، و زمان است. بهترین کاری که این نقشه‌راه می‌تواند بکند این
است که همه‌ی پیش‌نیازهای فنی/محتوایی یک بازی باکیفیت را آماده کند (فازهای بالا) تا وقتی
منتشر شد شانس رتبه‌ی خوب داشته باشد؛ این را در روادمپ بندی نکردیم چون خارج از حیطه‌ی کد است.
